# Phase 17 Planning: In-Memory Storage

**Date:** 2026-03-30
**Status:** approved
**Source:** `development/design/phase-17-storage-revised.md`
**Progress:** `.tdd-progress.md`

## Plan Summary

- 3 slices, 18 tests
- +3 C shim functions (141 → 144), 0 new API classes
- 1 new CLI example: z_storage
- Light feature phase — key expression matching + composition CLI

## Slice Map

| Slice | Description | C Fns | Tests | Depends |
|-------|-------------|-------|-------|---------|
| 1 | KeyExpr matching (intersects, includes, equals) | 3 | 12 | none |
| 2 | z_storage CLI startup | 0 | 3 | Slice 1 |
| 3 | z_storage end-to-end integration | 0 | 3 | Slice 2 |

## Dependency Graph

```
Slice 1 (KeyExpr matching)  ──>  Slice 2 (z_storage startup)  ──>  Slice 3 (Integration)
```

## Key Decisions

1. **No `zd_sample_clone`** — Sample is a pure Dart object; storage uses `Map<String, Sample>` with GC cleanup
2. **z_delete.dart workaround** — z_delete CLI lacks connect/listen flags; Test 17 uses in-process `Session.deleteResource()` instead of launching the CLI
3. **3 C shim functions** — `zd_keyexpr_intersects`, `zd_keyexpr_includes`, `zd_keyexpr_equals` loan views internally via `zd_view_keyexpr_loan()`
4. **No new Dart classes** — Methods added to existing `KeyExpr` class
5. **Storage is a CLI example, not a library class** — Reusable `Storage` class deferred
