name: exp_hooks_prebuilt_native
description: >-
  Experiment A2: Dart build hooks with prebuilt native libraries
  and @Native annotation loading mechanism.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  ffi: ^2.1.3
  hooks: ^1.0.0
  code_assets: ^1.0.0

dev_dependencies:
  test: ^1.25.0
