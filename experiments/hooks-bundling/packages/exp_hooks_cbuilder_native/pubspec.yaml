name: exp_hooks_cbuilder_native
description: >-
  Experiment B2: Dart build hooks with CBuilder (native_toolchain_c)
  compiling C shim from source, with @Native annotation loading mechanism.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  ffi: ^2.1.3
  hooks: ^1.0.0
  code_assets: ^1.0.0
  native_toolchain_c: ^0.17.5

dev_dependencies:
  test: ^1.25.0
