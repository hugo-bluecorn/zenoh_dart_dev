name: zenoh
description: "Dart bindings for the Zenoh pub/sub/query protocol via FFI."
version: 0.0.1
repository: https://github.com/hugo-bluecorn/zenoh_dart

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  code_assets: ^1.0.0
  ffi: ^2.1.3
  hooks: ^1.0.0

dev_dependencies:
  ffigen: ^20.1.1
  lints: ^5.1.0
  test: ^1.25.0
